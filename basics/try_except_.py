# ==========exceptions=========
# исключение -это обьекты которые прирывают работу кода,если они не были обработаны 
# 'syntaxError'-это исключение которое выходит когда в коде допущена синтакическая ошибка
# 'NameError'- это исключение которое выходит когда мы обращаемся к несуществуещей переменной
# 'print(adads)'
# 'indexError'-это мсключение которое выходит при обращении к несуществуюшему индексу 
# list_=[1,2,3,4,]
# list_=[10000]
# 'KeyError' - исключение которое выходит при обращении к несуществующему ключу 
# dict_{'a':1,'b':2}
# print(dict_['c'])
# 'ValueError'- когда мы передаем функцию не корректно для нее тип данных 
# int('10d')
# также выходит когда мы распаковываем иттерируем обьект на несколько переменных и количество переменных не совпадают с количеством значений 
#  a,b,c=[1,2]
#  'TypeError'-  когда мы пытаемся использовать методы не свойственные какому-то типу данных 
#  также выходит когда мы пытаемся передать функцию больше или меньше аргументов чем принимает функция 
#  for i in 1234567
# 5+'5'.-нельзя складовать строку и число 
# {[1,2,3]:'a'}

# 'ZiroOivisionError'- это исключение выходит когда делим что-то на 0,например 45/0,100/0,3%0
# 'AttributeError'- выходит когда  мы обращаемся к несушеств аттрибуту или методу обьекта (тип данных)
# [1,2,3].perlace(1,5)

# 'IndentationError'- выходит когда мы не правильно используем оступы
#      a=5
#       for i in range(10)print(i) 
# Exception -исключение которое создали чтобы его вызывать 

# ===========Вызов исключений===============
# raise NameError ('Я вызвал NameError')

# ===========Try except==================
# конструкция которая помогает обрабатывать исключения 
# try: #конструкция try используют если разработчик не уверен или знает что в коде есть ошибка и хочет обработать ее в except 
#     num=int(input('Введите число:'))
# except ValueError #нужна для обработки исключения в данном случае исклчючение valueError 
# print('Введите число а не символы')
# else:# срабатывает когда не вышла ошибка,исключения (когда не сработал except)
#     print(f'Вы ввели число {num} ')
# finally: #finally работает всегда 
#     print('досвидание')


# try:
#     print(number)
# except  (NameError,ValueError,TypeError):
#     print('Нет такой переменной')

# try:
#     raise Exception
# except:# отлавливаются все ошибки 
#     print('Отловлена любая ошибка')



# try:
#     raise  ValueError
# except Exception:# отлавливаются все ошибки 
#     print('Отловлена любая ошибка')

